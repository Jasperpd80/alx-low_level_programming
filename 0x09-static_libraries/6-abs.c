#include <stdlib.h>
#include "main.h"

/**
 *_abs - function that computes the absolute value of an integer.
 *@n: int
 *Return: @value
 */

int _abs(int n)
{
	/* if (n > 0) */
	/* { */
	return (abs(n));
	/* } */
	/* else if (n == 0) */
	/* { */
}
